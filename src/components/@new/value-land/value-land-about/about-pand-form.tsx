"use client";

import SimpleBar from "simplebar-react";

const AboutLandForm = () => (
  <SimpleBar className="h-full">
    {new Array(30).fill(0).map((el) => (
      <p>qwdqwd</p>
    ))}
  </SimpleBar>
);

export default AboutLandForm;
