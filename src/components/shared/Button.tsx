const Button = () => {
  const p = 2;
  console.log(p);

  return (
    <button
      type="button"
      className="bg-green text-dark-green font-semibold p-4 rounded-lg"
    >
      buttonqwdqdq
    </button>
  );
};

export default Button;
