import clsx from "clsx";
import React from "react";

const loading = () => (
  <div className={clsx("p-8 font-semibold text-center ")}>
    {/* <LoadingIcon1 className="m-auto flex !w-12 !h-12 fill-primary-main" /> */}
    Loading...
  </div>
);

export default loading;
