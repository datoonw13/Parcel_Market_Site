import { Box } from "@mui/material";
import React, { ReactElement } from "react";

const FindPropertyLayout = ({ children }: { children: ReactElement }) => <Box sx={{ height: "100vh" }}>{children}</Box>;

export default FindPropertyLayout;
